version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run prisma generate
    build:
      commands:
        - npm run production:build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/* 